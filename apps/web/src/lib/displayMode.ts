import type { DisplayMode } from '@openzcad/viewport/types';

export const DISPLAY_MODE_LABELS: Record<DisplayMode, string> = {
  'shaded-edges': 'Shaded + edges',
  shaded: 'Shaded',
  wireframe: 'Wireframe'
};

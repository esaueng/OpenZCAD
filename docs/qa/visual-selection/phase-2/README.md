# Phase 2 visual QA

The paired screenshots exercise a kernel-proven through-hole from two oblique
camera poses. The selected bore keeps its shaded cyan wall and physical rim
circles while the dim hidden pass and gray-green analytic cylinder expose the
full axis, diameter, and depth through the surrounding solid.

Regenerate with:

```sh
OZ_QA_EVIDENCE=1 pnpm exec playwright test test/e2e/visual-selection-xray.spec.ts
```

name: Legacy VPS pilot (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  migrated:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Trusted checks now run through the main CI workflow.'
